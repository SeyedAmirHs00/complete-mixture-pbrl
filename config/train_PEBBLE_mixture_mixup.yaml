defaults:
    - base_mixture
    
# this needs to be specified manually
experiment: PEBBLE_mixture_mixup

# Label Noise Correction
mixup_alpha: 8
original_concat: True

# hydra configuration
hydra:
    run:
        dir: ./exp_pebble_mixture_mixup/${env}/seed${seed}/max_feedback${max_feedback}_n${reward_batch}_l${segment}_g${teacher_gamma}_b${teacher_beta}_m${teacher_eps_mistake}_s${teacher_eps_skip}_e${teacher_eps_equal}
